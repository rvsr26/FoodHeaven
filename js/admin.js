console.log("Admin Dashboard scripts loaded");
export function addMenuItem(item) { console.log("Added item", item); }
export function deleteMenuItem(id) { console.log("Deleted item", id); }
// CRUD comments added
// Payloads compression hook
// fallback image config
